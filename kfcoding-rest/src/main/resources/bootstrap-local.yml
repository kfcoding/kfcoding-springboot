spring:
  application:
    name: kfcoding-rest

info:
  name: rest端


logging:
  file: target/${spring.application.name}.log

kfcoding:
  exception:
    frontendMessage:
      51000: 登录失效，请重新登录
      60001: 实体券需要开始和结束编码号

<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.cuiyun.kfcoding.rest.modular.course.dao.KongfuMapper">

    <resultMap id="BaseResultMap" type="com.cuiyun.kfcoding.rest.modular.course.model.Kongfu">
        <id column="id" property="id" />
        <result column="title" property="title" />
        <result column="author" property="author" />
        <result column="type" property="type" />
        <result column="brief" property="brief" />
        <result column="created_at" property="createdAt" />
        <result column="user_id" property="userId" />
        <result column="level" property="level" />
        <result column="surface_image" property="surfaceImage" />
        <result column="surface_background" property="surfaceBackground" />
        <result column="status" property="status" />
        <result column="priority" property="priority" />
        <result column="is_del" property="isDel" />
        <collection property="tags" ofType="com.cuiyun.kfcoding.rest.modular.course.model.Tag" select="getTags" column="id">
        </collection>
    </resultMap>

    <sql id="Base_Column_List">
        id, title, author, type, brief, created_at AS createdAt, user_id AS userId, level, surface_image AS surfaceImage, surface_background AS surfaceBackground, status, priority, is_del AS isDel
    </sql>

    <sql id="Column_List">
        k.id, k.title, k.author, k.type, k.brief, k.created_at, k.user_id, k.level, k.surface_image, k.surface_background, k.status, k.priority, k.is_del
    </sql>

    <select id="getKongfuById" resultMap="BaseResultMap">
        SELECT
        <include refid="Column_List" />
        FROM course_kongfu k
        WHERE k.id=#{id}
    </select>

    <select id="getTags" parameterType="java.lang.String" resultType="com.cuiyun.kfcoding.rest.modular.course.model.Tag">
        SELECT t.id, t.name
        FROM course_kongfu k
        INNER JOIN course_kongfu_to_tag ktg ON k.id=ktg.kongfu_id
        INNER JOIN course_tag t ON ktg.tag_id=t.id
        WHERE k.id=#{id}
    </select>

    <select id="getKongfuByTag" resultMap="BaseResultMap">
        SELECT
        <include refid="Column_List" />
        FROM course_tag t
        LEFT JOIN course_kongfu_to_tag ktg ON t.id=ktg.tag_id
        LEFT JOIN course_kongfu k ON ktg.kongfu_id=k.id
        WHERE t.id=#{id} AND k.status=#{status}
    </select>

</mapper>
